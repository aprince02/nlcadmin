<%- include("_loggedInHeader") -%>

<h1>All Donations</h1>

<div class="table-responsive-sm">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Date</th>
          <th>First Name</th>
          <th>Surname</th>
          <th>Amount</th>
          <th>Fund</th>
          <th>Notes</th>
          <th>Gift Aid Status</th>
        </tr>
      </thead>
      <tbody>
        <% for (const payments of model) { %>
          <tr>
            <td><%= payments.date %></td>
            <td ><%= payments.first_name %></td>
            <td ><%= payments.surname %></td>
            <td>£<%= payments.amount %></td>
            <td><%= payments.fund %></td>
            <td><%= payments.notes %></td>
            <td><%= payments.gift_aid_status %></td>
            <td class="d-print-none">
              <a class="btn btn-sm btn-warning" href="/edit-donation/<%= payments.id %>">Edit</a>
            </td>

          </tr>
        <% } %>
        <a class="btn btn-default btn-success" href="/select-giver">Add</a>
        <a class="btn btn-outline-dark cancel" href="/claimants">Return</a>
      </tbody>
    </table>
    
  </div>
  
  <%- include("_loggedInFooter") -%>